import React from 'react';
// import react router, switch, links, etc
// import react useState and useEffect

// create stateful Item component to include:
/*state = {
  itemLocation: '',
  ownedBy: '', // how do we relate this to user by _id?
  claimed: false,
  address: '',
}*/

// useEffect(() => {
// fetch request
// set items with new state
// catch error
// });

// claimed function

// render
// map singular character with query
// onClick for claimed to change state

const Item = () => {
  return <div />;
};

export default Item;
