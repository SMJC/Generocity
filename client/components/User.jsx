import React from 'react';
// import react router, switch, links, etc
// import react useState hooks
// import Item component

// create User component to include:

// useEffect(() => {
// fetch request
// set items with new state
// catch error
// });

// render
// set the item as state
// map characters with query
// load all items, demarcate what is claimed

const User = () => {
  return <div />;
};

export default User;
